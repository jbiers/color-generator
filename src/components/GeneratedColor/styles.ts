import styled from "styled-components"

export const Color = styled.div`
    background: var(--blue);

    margin: 3.125rem auto;

    width: 14.125rem;
    height: 14.125rem;
    border-radius: 50%;
`